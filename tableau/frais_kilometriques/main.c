#include <stdio.h>
#include <stdlib.h>

int main()
{
    int cv,d;
    int tab [5][4] =   {    {3, 0.451, (0.270)+906 , 0.315} ,
                            {4, 0.518, (0.291)+1136, 0.349} ,
                            {5, 0.543, (0.305)+1188, 0.364} ,
                            {6, 0.568, (0.32)+1244 , 0.382} ,
                            {7, 0.595, (0.337+1288), 0.401} }}
    printf ("Entrez le nombre de CV de votre voiture : ");
    scanf ("%d", &tab[cv][1]);

    printf ("Entrez la distance en kil : ");
    scanf ("&d", &d);

    for (int i = 0; i<=5; i++)
    {
        for (int j = 0, j <=4, j++)
        {
            if (cv )
        }
    }
    return 0;
}
